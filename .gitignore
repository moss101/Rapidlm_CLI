# Rust
/target/
**/*.rs.bk

# Node / pnpm (SDK workspace)
node_modules/
.pnpm-store/
dist/
coverage/
*.tsbuildinfo

# Local orchestration state (keep workflow scripts)
.grok/rapidlm-v2/

# Cross-process advisory lock for .rapidlm/goal.json (see GoalHost::update /
# GoalLock) — a transient coordination file with no meaningful content,
# never itself part of the persisted goal contract.
.rapidlm/goal.lock

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
*~
