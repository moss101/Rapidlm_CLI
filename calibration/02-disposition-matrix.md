# C. V2 → V3 Component Disposition Matrix

Columns: Component · Path · Observed capability · Evidence · Tests · V3 target · Status · Disposition · Gap · Next V3 IDs

Disposition key: KEEP · ADAPT · REPLACE · DELETE · INVESTIGATE. REPLACE/DELETE are calibration conclusions only — not performed in this goal.

| Component | Path | Observed capability | Evidence | Tests | V3 target | Status | Disposition | Gap | Next IDs |
|---|---|---|---|---|---|---|---|---|---|
| Kernel runtime | `crates/kernel` | Lifecycle/service-graph, sessions, recovery, config+security merge, project trust, IPC, client | `service_graph.rs:136`, `session/service.rs:96`, `recovery/mod.rs:88`, `ipc/server.rs:61` | many (crash_recovery.rs) | GraphService authority host | VERIFIED_EXISTING | KEEP/ADAPT | daemon IPC↔token auth uncomposed; no GraphService yet | P1, P2 |
| Event Ledger | `crates/event-ledger` | SQLite ledger, CAS artifact store, checkpoints, subscriptions, migrations | `ledger.rs:181 append`, `artifact_store.rs:69`, `migrations.rs:4` | yes | graph-revision events | VERIFIED_EXISTING | KEEP/EXTEND | checkpoints lack dedicated unit test | P1 |
| Protocol | `crates/protocol` | IDs, config schema, error, artifact, repo_path, trace, remote_worker | `id.rs:30`, `config.rs`, `trace.rs:58`, `remote_worker.rs` | yes | wire authority | VERIFIED_EXISTING | KEEP | none | — |
| Auth | `crates/auth` | local_daemon token, secret handles, mTLS, store | `local_daemon.rs:307`, `secret.rs`, `mtls.rs:386`, `store.rs:711` | yes (in-memory) | durable OS-keychain creds | PARTIAL | KEEP/ADAPT | `UnimplementedPlatformKeychain` stub; mTLS unverified | P1, P4 |
| Agent runtime (harness+goals) | `crates/agent-runtime` | turn loop, subagents, isolated views, budgets, cancel, goal lifecycle, evidence/verifier, compaction | `turn.rs`, `agent/scheduler.rs`, `goal/state.rs`, `evidence.rs`, `compaction.rs` | yes (~110 types) | AgentNodeExecutor; loop detection; repair | VERIFIED + PARTIAL | KEEP/EXTEND | loop detection MISSING; tool-call repair MISSING; persistent-agent lifecycle absent | P5, P1, P6 |
| Context engine | `crates/context-engine` | FTS/BM25, tree-sitter, LSP enricher, code graph, vector/hybrid, MMR, compiler, durable memory, read-set | `index/fts.rs:58`, `parse/symbols.rs`, `lsp/mod.rs`, `compile.rs`, `memory.rs` | ~148 unit | Context Fabric; ripgrep; negative findings; test selection | VERIFIED + PARTIAL | KEEP/EXTEND | ripgrep absent; negative-findings & test-selection absent; embeddings hash-only | P3 |
| Workspace | `crates/workspace` | semantic/byte patches, transactions, external-mutation detection, attribution, conflict/rollback, direct+git_worktree backends | `patch/model.rs`, `transaction.rs`, `external_mutation.rs`, `backends/direct.rs`, `git_worktree.rs` | yes | overlay/remote backends; rewind | PARTIAL | KEEP/EXTEND | `Overlay`/`Remote` backends declared, unimplemented; no textual diff renderer | P4 |
| VCS / provenance | `crates/vcs` | in-memory provenance graph (agent/evidence/goal→patch→commit edges) | `provenance.rs` | yes (in-memory) | durable attestation/signature issuance | PARTIAL | KEEP/EXTEND | no cryptographic attestation/signature; durable persistence delegated | P4 |
| Tool gateway | `crates/tool-gateway` | registry, JSON-schema validation, bounded/artifact output, error envelopes | `schema.rs`, `validate.rs`, `dispatch.rs`, `bound_output` | yes | validate-repair-revalidate; cross-tool invariants | VERIFIED + PARTIAL | KEEP/EXTEND | tool **repair** module absent; dedicated recovery absent | P4 |
| Capability broker | `crates/capability-broker` | policy engine, broker, lease issue/verify, executor-side validation, approvals, audit, scanners, redaction, network policy, doctor, gate | `policy/evaluator.rs:389`, `lease.rs:99/119`, `validator.rs:189`, `gate.rs`, `doctor.rs` | 143 | dont-ask transform; credential broker wiring | VERIFIED + PARTIAL | KEEP/EXTEND | `Ask→Deny` transform not in scope crates; `SecretBrokerToken` dead_code | P4 |
| Sandbox | `crates/sandbox` | host/container/gVisor/remote backends, resource leases, process trees, cancel, network isolation | `backends/{host_restricted,container,gvisor,remote}.rs`, `backend.rs:1053 select` | 86 | warm ResourcePool | VERIFIED + MISSING | KEEP/EXTEND | **warm pool** absent | P4 |
| Process supervisor | `crates/process-supervisor` | detached/background jobs, registry, cron, job leases, recovery, cancel tree | `jobs.rs`, `schedule.rs`, `recovery.rs`, `cancel.rs` | 92 | wake-on-event, triggers, monitors, jitter | VERIFIED + PARTIAL | KEEP/EXTEND | monitors/wake-on-event/triggers/jitter **absent** | P4, P7 |
| LLM router | `crates/llm-router` | Anthropic+OpenAI adapters, catalog, routing, fallback, cost/latency/context constraints, tool-cap matching | `providers/*.rs`, `catalog.rs`, `route/{filter,score}.rs`, `fallback.rs` | yes | TLS transport; vision; structured output | VERIFIED + PARTIAL | ADAPT | **no TLS**→cloud unreachable; vision bytes not embedded; structured-output not transmitted; only 2 providers | P8 |
| MCP | `crates/mcp` | catalog, gateway(client), server, transport, trust | `gateway.rs:259`, `server.rs:608`, `transport.rs`, `trust.rs` | yes | external-agent adapter | VERIFIED_EXISTING | KEEP | none | P7, P9 |
| ACP | `crates/acp` | stdio framing, v1, v2 negotiation | `stdio.rs`, `v1.rs:79`, `v2.rs:336` | yes (compat) | external-agent adapter | VERIFIED_EXISTING | KEEP | none | P7, P9 |
| Plugin host | `crates/plugin-host` | hooks, skills, manifest/install, **hand-rolled WASM interpreter** | `hooks.rs`, `skills.rs`, `manifest.rs`, `install.rs`, `wasm.rs:385` | yes | WASM sandbox | VERIFIED_EXISTING | KEEP/INVESTIGATE | WASM is bespoke interpreter (i32/i64, bounded ops) — correctness risk; WASI never linked | P4, P9 |
| TUI | `crates/tui` | 7 panels (golden-tested), command parse/dispatch, state reducer | `panels/*`, `commands.rs`, `tests/ui_snapshots.rs` | golden | paint + graph/evidence panels | VERIFIED + PARTIAL | ADAPT | binary never paints (no ratatui dep); graph/evidence panels missing; commands no-op in loop | P9, P10 |
| Telemetry | `crates/telemetry` | redacted export, manifest | `export.rs` | yes | session insights feed | VERIFIED_EXISTING | KEEP | none | P10 |
| Insights | `crates/insights` | — | empty `#![forbid(unsafe_code)]` | none | Session Insights engine | STUB | INVESTIGATE/BUILD | entire crate empty | P10 |
| Playbooks | `crates/playbooks` | — | empty `#![forbid(unsafe_code)]` | none | playbook→graph compiler | STUB | INVESTIGATE/BUILD | entire crate empty | P4 |
| Eval harness | `crates/harness` | — | empty `#![forbid(unsafe_code)]` | none | Flight Simulator (ScriptedModel/Replay/Live/FaultInjector/Graders) | STUB | INVESTIGATE/BUILD | entire crate empty; only in-test mocks exist | P10, P11 |
| Scheduler (runtime graph) | `crates/scheduler` | — | empty `#![forbid(unsafe_code)]` | none | Runtime Graph scheduler (ready-set, fan-out, barriers, joins, fairness) | STUB | INVESTIGATE/BUILD | empty — V3-designated home for graph scheduler | P2 |
| Handoff | `crates/handoff` | — | empty `#![forbid(unsafe_code)]` | none | HandoffBundle, SessionExecutionLease, split-brain prevention | STUB | INVESTIGATE/BUILD | empty; only protocol ID scaffolding exists elsewhere | P1, P2, P7 |
| Agent pool | `crates/agent-pool` | — | empty `#![forbid(unsafe_code)]` | none | managed worker pool | STUB | INVESTIGATE (build or DELETE) | empty; `agent-runtime::agent::scheduler` already covers agent scheduling | P5 |
| Trajectory | `crates/trajectory` | — | empty `#![forbid(unsafe_code)]` | none | experiment registry / learning flywheel | STUB | INVESTIGATE/BUILD | entire crate empty | P10 |
| Knowledge | `crates/knowledge` | — | empty `#![forbid(unsafe_code)]` | none | Knowledge/Preference/Rule registry | STUB | INVESTIGATE/BUILD | empty; durable memory already in `context-engine::memory` — ownership must be reconciled | P5 |
| Computer use | `crates/computer-use` | browser observe/act/verify/trace/security logic; desktop orchestration; mobile adb/xcrun | `browser/*`, `desktop/backend.rs`, `mobile-sim/*` | yes (Scripted/Fake) | live CDP/Playwright; macOS AX/Win UIA/Linux AT-SPI | PARTIAL | ADAPT | all `Live*` desktop/Playwright drivers **fail-closed** (`#![forbid(unsafe_code)]` blocks OS frameworks) | P6, P8 |
| Mobile sim | `crates/mobile-sim` | Android (adb/AVD) + iOS (xcrun simctl) real shell-out; trace | `android/*`, `ios/simctl.rs`, `trace.rs` | yes | remote worker delegation | VERIFIED_EXISTING | KEEP | iOS live off-macOS fail-closed (by design) | P6, P8 |
| App (rapid) | `apps/rapid` | composition root, interactive loop, headless JSONL emitter | `interactive.rs`, `headless/jsonl.rs`, `main.rs` | yes (jsonl golden) | wire TUI render + all commands + headless entry | PARTIAL | ADAPT | loop no-ops most kernel commands; headless `run()` rejects subcommands; no paint | P9, P10 |
| TS SDK | `sdk/typescript` | client, wire types, transport, tests | `src/client.ts`, `generated/index.ts`, `test/*` | yes | public SDK | VERIFIED_EXISTING | KEEP | none | P9, P10 |
| Build / CI | root | cargo workspace; CI fmt/clippy/test/typecheck | `Cargo.toml`, `.github/workflows/ci.yml` | CI runs checks | packaging, signing, SBOM, provenance, release channels, doctor, security scan | PARTIAL | ADAPT | no packaging/signing/SBOM/provenance/release/doctor/audit in CI | P11 |

## Disposition tally

- **KEEP / KEEP+EXTEND / ADAPT (preserve & evolve):** kernel, event-ledger, protocol, auth, agent-runtime, context-engine, workspace, vcs, tool-gateway, capability-broker, sandbox, process-supervisor, mcp, acp, plugin-host, tui, telemetry, mobile-sim, apps/rapid, sdk/typescript, build/CI, llm-router (ADAPT for TLS), computer-use (ADAPT for live drivers).
- **INVESTIGATE / BUILD (currently empty shells):** scheduler, handoff, agent-pool, trajectory, knowledge, insights, playbooks, harness.
- **REPLACE / DELETE:** none recommended at calibration time. `agent-pool` is the only candidate for DELETE-or-merge into `agent-runtime`, pending the next goal's decision. Do **not** delete any working primitive merely because a V3 name differs (per `docs/research/migration-v2-to-v3.md`).
