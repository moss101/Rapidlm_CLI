# Computer / Browser / Mobile / Preview Evals

Fake deterministic drivers plus supported real integration. Test structured targeting preference, coordinate transforms/aspect mismatch, stale observation rejection, batching/settle, screenshot-on-failure, browser console/network errors, prompt-injection content, preview compile/HMR/redirect failures, human takeover, Android and remote-mac iOS. Criterion-specific visual evidence must be inspected/asserted, not just captured.
## Common gate
Run against production contracts. Prefer deterministic assertions before model judges. Record exact fixture/version/model/prompt/context/tool schemas. A security/data-loss/duplicate-effect/false-completion regression is a hard failure regardless of aggregate score.
