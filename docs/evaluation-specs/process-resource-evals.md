# Process, Job, Sandbox and Resource Evals

PTY and non-PTY, huge output, background detach, tree kill, timeout, user-process protection, monitor wake, no polling, cron duplicate firing, scheduler lease takeover, pool hit/miss, interrupted installation quarantine, worker disconnect, sandbox backend unavailable/no downgrade, resource sanitation and cross-identity isolation.
## Common gate
Run against production contracts. Prefer deterministic assertions before model judges. Record exact fixture/version/model/prompt/context/tool schemas. A security/data-loss/duplicate-effect/false-completion regression is a hard failure regardless of aggregate score.
