# SEAM-00-1 — Baseline audit and existing-work map for `SEAMS-RAPIDLM-01`

**Date:** 2026-09-20  
**Goal:** [`SEAMS-RAPIDLM-01`](seams-governing-principles.md), Phase 0 (§5.1)  
**Worklist entries covered:** SEAM-00-1 (this document), SEAM-00-2 (the ADRs linked in §6), SEAM-00-3 (the worklist, ledger entries and governing-document corrections)  
**Method:** every claim below was checked against the source tree at the baseline commit by reading the code or running `grep`. Each "today" paragraph of the governing document's §4 is rechecked line by line; where it is wrong, the correction is recorded here and in the worklist, and the governing document is amended only through a commit whose message names this audit (§0.3). Effort labels come from this audit, not from §4.

## 1. Baseline

| Fact | Value |
|---|---|
| Planning baseline named by the governing document | `89350d70b1f08e5a11ca0b80477436ad048711a4` (`fix(publication): publish from the real diff …`, 2026-09-19) |
| Baseline for this audit | the same commit; every `file:line` below is to that tree |
| Dirty working state at audit time (preserved untouched) | `.rapidlm/goal.json` (pre-existing local modification); untracked `docs/codebase-assessment-2026-09-15.md`, `docs/implementation-recheck-2026-09-15.md`, `eval/results/offline-1789495072.json`, `eval/results/offline-1789495145.json`; the two goal documents this program adds (`docs/goals/seams-governing-principles.md`, `docs/goals/seams-goal-prompt.md`) were untracked and are committed with this audit |
| Toolchain | `rust-toolchain.toml` 1.97.1; Node 24.19.0; pnpm 11.20.0 (pinned in `.github/workflows/ci.yml`) |
| Required checks | `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo test --workspace --locked --no-fail-fast`, `cargo test --locked -p protocol --test schema_fixtures`, `pnpm generate:check`, `pnpm typecheck`, `pnpm test` |

### Supported-platform contract (rechecked)

| Platform | Build + lint | Test suite | Notes |
|---|---|---|---|
| macOS (Apple silicon) | gate | gate | Seatbelt sandbox tier |
| Linux x86_64 | gate | gate | container/gVisor tiers when present; host-restricted tier via `ulimit`/`ps` |
| Windows x86_64 | gate | **gate** (`.github/workflows/ci.yml` "Test" step has no `continue-on-error`; comment dated 2026-09-17) | PTY and host-restricted sandbox report typed unavailability; daemon socket is Unix-only; hooks run under `cmd /C` (`apps/rapid/src/hooks.rs:130`) |

Every contract this program adds ships portable tests or a typed-unavailability test on Windows (§3.2 S8).

## 2. Cross-cutting ground truth (the seams every item attaches to)

| Primitive | Where | Reachable from `apps/rapid` | Notes for this program |
|---|---|---|---|
| Tool dispatch loop | `apps/rapid/src/exec_tools.rs` (`ExecTools::execute_call_traced_flagged`, permission at `permission_for` `:2176`, pre-hooks `:2353`, post-hooks `:2490`), driven by `crates/agent-runtime` `turn.rs` | yes — headless `exec_turn`, the TUI's `execute_interactive_turn`, ACP, subagents (`LiveSubagentRunner`, `interactive.rs:2902`) all run it | the one place a hook decision, a rewrite or an inbox message can apply |
| Durable Human/Approval wait | `apps/rapid/src/approvals.rs`; kernel `ApprovalRequestedPayload { id, call_id, tool, summary, scope, diff, detail }` (`crates/kernel/src/client.rs:662–675`) | yes (TUI `/approvals`, ACP `acp_resolve_and_continue`) — **not** headless `rapid exec`, which builds tools without a sink (`interactive.rs:8633–8636`) and turns `Ask` into a model-visible `Denied(ModeAsk)` | events in order: `approval.requested` → `tool.approval_required` (carries the `SuspendedTurn`) → `turn.interrupted[ApprovalPending]` → `approval.resolved` → continuation turn. `JsonlExitCode::NeedsApproval = 10` exists (`headless/jsonl.rs:104`) but is used only by `rapid run` |
| Permission lattice | `apps/rapid/src/permissions.rs`: six modes; `Plan` denies every non-read-only class before any rule (`:590`) | yes | plan mode's read-only guarantee already lives in policy |
| Runtime graph | `crates/scheduler`: `NodeKind` includes `Agent, Process, Monitor, Approval, Plan, WorkspaceTransaction`; `NodeState` includes `Waiting, Paused`; `EdgeKind` has `DependsOn, RequiresApproval, Supersedes, TriggeredBy, DelegatedTo`; `GraphService::replay` since 2026-09-18 | only through `rapid run --orchestration verified` (`apps/rapid/src/workflow_verified.rs`) | there is no message/inbox edge kind |
| Cron | `scheduler::PromptCron` over the ledger's `cron_jobs` table; `rapid cron add|list|remove|poll` (`p9_commands.rs:1607`), fired prompts run as `PermissionMode::Plan` turns | yes | the `/loop` substrate |
| Agent definitions | `agent_runtime::agent_defs` (`.rapidlm/agents/*.toml`, closed schema, grants must be a subset of the role surface, builtins cannot be shadowed) via `rapid agents list|validate|scaffold` (`p9_commands.rs:2196`) | listing only — the spawn path (`LiveSubagentRunner::run`) keys on the free-text `agent_type` and never consults the definitions | the role-overlay substrate |
| Ledger event kinds | `crates/event-ledger/src/event.rs:142–274`; `Job*`, `AgentMail{Sent,Dropped}`, `AgentPoolBackground*`, `Message{Queued,State}` exist; `AgentMail*` has **no emitter** anywhere | yes | the SDK catalog `sdk/typescript/schemas/wire.v1.json` is hand-maintained; no Rust→catalog drift test exists |
| Schema fixtures | `crates/protocol/tests/schema_fixtures.rs`; fixtures at `tests/fixtures/<family>/v<n>/<name>.json`; 3 today (`id`, `error`, `artifact`); rewrite only with `UPDATE_SCHEMA_FIXTURES=1` | n/a | every new schema in this program adds a fixture here |
| Tool argument validation | per-tool `parse_*_args` at execution (`exec_tools.rs:2371–2385`); `crates/tool-gateway` is **not** a dependency of `apps/rapid` and `ExecTools::validate` is a pass-through (`:6927–6938`) | — | "validate against the tool contract" means the per-tool parsers today |
| Untrusted-content fence | `<untrusted_context locator="…">` for `TrustClass::Untrusted` context blocks (`apps/rapid/src/model.rs:530–537`); tool results themselves are plain `Tool` messages with no fence or provenance tag | yes (context only) | the fence to reuse for hook context, monitor lines and search results |
| Output bounds | `MAX_RESULT_DETAIL_BYTES = 256`, `MAX_READ_BYTES = 4 KiB`, `MAX_SEARCH_OUTPUT_BYTES = 8 KiB`, `MAX_SHELL_OUTPUT_BYTES = 16 KiB`, `MAX_JOB_OUTPUT_BYTES = 64 KiB`, `MAX_TOOL_ARGUMENTS_BYTES = 8 KiB` (`exec_tools.rs`) | yes | there is no single "tool-output ceiling"; each item names the bound it reuses |
| Subcommands | `SUBCOMMANDS` (`interactive.rs:583–780`): `exec update eval daemon acp run trust resume goal playbook-compile mcp mcp-tools tools agent-cli permissions doctor sessions inspect-export insights cron scan browser findings agents plugins release-manifest completions man` | — | `docs/reference/cli-command-reference.md:41` advertises `rapid hooks list|test|enable|disable`, which does not exist (S9 correction when that file is touched) |
| Project settings | `PROJECT_SETTINGS_FILES = [".rapidlm/settings.json", <one peer-host compatibility path>]` (`interactive.rs:2148–2149`), trust-gated | yes | the compatibility path is left as is and not extended (SEAM-06 note applies to hooks too) |

## 3. Item-by-item recheck of §4

Each subsection: **claims** (CONFIRMED / CORRECTED / NOT FOUND with evidence), **reachability**, **real remaining work**, **effort** (S = a day or less; M = a few days; L = a week or more of serial slices).

### SEAM-01 — Hook decision contract v2

| Claim in §4 | Verdict | Evidence |
|---|---|---|
| `pre_tool_use` hooks receive the call as JSON on stdin; non-zero exit denies with stderr as detail; a hung hook denies | CONFIRMED, with a correction to "stderr": both streams go to one file | `apps/rapid/src/hooks.rs:224–253` (`run_pre_tool_hooks`), `:153–221` (`run_hook_once` redirects stdout **and** stderr into one temp file; timeout → `"hook timed out"`) |
| `post_tool_use` output recorded on the result | CONFIRMED | `exec_tools.rs:2490–2504` appends `[post_tool_use: …]` |
| `HookEvent` covers SessionStart/SessionEnd/TurnStart/TurnEnd/Stop/Notification | **CORRECTED** — 27 variants, no `Stop`/`Notification` | `crates/plugin-host/src/hooks.rs:154–182`; blocking set at `:430–443` |
| `HookDecision { Continue, Block }`, `HookDisposition { Continue, Warn, Block }`, `HookKind { Command, Http, Plugin }`; capabilities + sandbox per hook | CONFIRMED | `:270–281`, `:190–194`, `HookSpec` `:222–231` |
| `managed_config.rs` has no hook gate | CONFIRMED | `grep -i hook` → 0; closed `[policy]` key set `:248–265` |
| (implicit) the plugin-host engine is what production runs | **CORRECTED** — it has zero production callers; `rapid plugins hook-test` is a dry-run | `p9_commands.rs:2516–2563`; the exec loop uses `apps/rapid/src/hooks.rs` only |
| (implicit) a hooks reference page exists to update (AC-08) | **NOT FOUND** — no `docs/reference/*hook*`; the only `pre_tool_use` mention in `docs/` is the governing document | `ls docs/reference`; `grep -ril pre_tool_use docs` |

Additional facts: the production stages are `pre_tool_use, post_tool_use, session_start, session_end, subagent_start, subagent_stop, pre_compact, post_compact` (`hooks.rs:23–46`); nothing fires on turn interruption (`TurnEvent::Interrupted` is dropped by the TUI sink, `interactive.rs:7718–7721`; the kernel appends `turn.interrupted`); the plugin-host parser already reads `{"decision":"block"}` from stdout and flags grant-shaped keys (`:1190–1214`); `hooks.rs:1,14` and `interactive.rs:2147` carry a peer-product name (rule 2.4 applies when touched).

**Reachable:** project-settings shell hooks behind the trust gate; the durable approval wait (TUI/ACP). **Unwired:** `plugin_host::hooks::dispatch`, `tool_gateway::validate`. **Missing:** a structured result channel (streams are merged), `ask`/`defer`/rewrite/context semantics, the four new events, managed gates, a hooks reference page, any hook/approval schema fixture, provenance tagging of tool output.

**Real remaining work** (ADR 0022): separate the streams and parse `protocol::hooks::HookResult` v2; map `ask` onto `Decision::Ask` and give headless exec a recording sink + `NeedsApproval`; rewrite validation through the per-tool parsers with `hook.input_rewritten` + transcript marker; fenced, bounded `additional_context`; stages `user_prompt_submit`, `stop`, `stop_cancelled`, `post_tool_use_failure`, blocking `subagent_stop`; `hooks.managed_only`/`hooks.denied_events` gates; `docs/reference/hooks.md`; fixture `hooks/v2/hook_result.json`. **Effort: L** (five serial slices; the headless approval sink is the only piece with a design question, settled in ADR 0022 §3).

### SEAM-09 — Permission UX

| Claim in §4 | Verdict | Evidence |
|---|---|---|
| `RAPIDLM_PERMISSION_MODE` | CONFIRMED | `interactive.rs:2145`, resolution `:2171–2193` (env > settings `mode`/`permissions.defaultMode` > `default`) |
| `rapid permissions list|allow|revoke` | CONFIRMED | `permissions_cli.rs:153–163`; store `<home>/project-permissions.json` keyed by canonical root (`:227–256`) |
| deny rules in `permissions.rs` | CONFIRMED | `RuleEffect::Deny`, `parse_settings` `:687–700`, `evaluate` `:596–600` |
| managed `max_permission_mode`, `denied_tools` | CONFIRMED | `managed_config.rs:168–171`, `:452–458` |
| approvals in `approvals.rs` | CONFIRMED | see §2 |

Additional facts: shell rules match `argv.join(" ")` with `*`/`?` glob only (`permissions.rs:171–254`; `exec_tools.rs:2155–2157`); a bounded shell tokenizer exists in `crates/security/src/scanners/command.rs:990` (`tokenize_shell`) but is advisory text only (`exec_tools.rs:5220–5250`); "approve and remember" persists an allow grant for path tools and bare tool names, refuses `shell_exec` (`interactive.rs:9225–9234`, `:6132–6138`); there is no persisted "never allow" (`permissions_cli.rs:161–162` has `Allow|Revoke` only); ACP `allow_always` collapses to a one-shot approve (`acp_serve.rs:479–489`); `permissions list` shows persisted grants only, no origin; `Auto` equals `AcceptEdits` in the mode table (`permissions.rs:622–628`), no safe-command list; `parse_exec_args` has no `--allow`/`--permission-mode` (`interactive.rs:1973–2027`); `user_config.rs` has no permission key; the production approval modal is three lines with no diff or script (`crates/tui/src/compositor.rs:679–698`) while a rich `ApprovalViewModel` (`crates/tui/src/panels/approval.rs`) is exported, snapshot-tested (`ui_snapshots.rs:1074`) and **unused by production**.

**Real remaining work:** persisted deny answers (+ origin in `permissions list`); an AST-aware matcher built on `security::tokenize_shell`; a safe-command list for `auto`; wire the existing rich approval view model into `paint_screen` (diff + script); `rapid exec --allow` bounded by the managed ceiling; default mode in user config. **Effort: M.** Depends on SEAM-01 only for the shared `ask` path.

### SEAM-02 — Provider onboarding and model-call resilience

| Claim in §4 | Verdict | Evidence |
|---|---|---|
| Configuration is hand-written; the reference page documents `[models]`, `[model.<id>]` with `provider`, `model`, `base_url`, `env_key` | CONFIRMED, and the page lags the parser | the only parser is `user_config.rs:389` `parse_config_document`; no production writer of `config.toml` exists (only tests at `user_config.rs:1469,1486`). `[model.<id>]` accepts 12 keys (`:479–492`): `provider, model, base_url, name, api_key, env_key, max_tokens, context_window, reasoning_effort, vision, caching, reasoning`; `[models] fallback` (max 7) and `[phases]` exist. `docs/reference/model-configuration.md:43–52` lists 8 keys and omits the other four, `fallback` and `[phases]` |
| Two wire dialects under `providers/` | CONFIRMED | `crates/llm-router/src/lib.rs:8–11`: `openai_compatible.rs` (ChatCompletions + an unreachable `Responses` style, `model.rs:245–248` pins ChatCompletions) and `anthropic.rs` (Messages API) |
| `crates/auth` OS/file keychains, `CredentialKind::{ProviderApiKey, OauthRefresh, OauthAccess}`; `credentials.rs` resolves; `mtls.rs` exists | CONFIRMED for existence; **CORRECTED** for use — the keychain is never used for provider credentials | `store.rs:64–71` (also `DaemonToken, WorkerCert, Generic`); at request time `resolve_credential` (`user_config.rs:805–826`: inline `api_key` > `env_key` > keyless) seeds an `InMemoryCredentialStore` (`model.rs:324–358`) and the same plaintext is baked into `StaticWireAuth::bearer` (`model.rs:231–241`); `doctor.rs:1349–1352` states provider credentials never come from the keychain |
| `rapid doctor` is offline and reports "unconfigured" | CONFIRMED offline; **CORRECTED** wording and exit code | `doctor.rs:17–20`; the model row is `WARN "no model configured (searched <path>)"` (`:380–385`) and the process exits **0** (`tests/doctor_cli.rs:414–421`); `docs/getting-started.md:57–58` claims exit 1. `doctor` accepts no arguments at all (`p9_commands.rs:1422–1429`) |
| Retry is one global knob `RAPIDLM_RETRY_BASE_MS` | **CORRECTED** — the knob lives in `apps/rapid/src/host.rs:990–996`, not the router; there are two retry layers | `SupervisedModel::step` retries `Transient`/`Connection`/`Rejected` up to `MAX_TRANSIENT_RETRIES = 5` (`host.rs:1099–1218`); `FallbackChainModel` uses `FallbackPolicy::standard()` (2 same-model retries, 200 ms base, 5 s cap; `fallback.rs:39–46`). No per-model override key |
| Catalog carries prices, regions, latency classes | CONFIRMED, unwired | `catalog.rs:52–61`, `provider.rs:255–259, 334–339`; no `apps/rapid` consumer (`CatalogRevision::new(1)` hardcoded at `model.rs:633`; `UsageAccumulator` has no consumer outside the router) |
| No presets, no live probe, no continuation, no per-effort ids, no per-model retry, no reasoning-summary | CONFIRMED (all six) | both adapters produce `FinishReason::Length` (`openai_compatible.rs:1624–1627`, `anthropic.rs:1004–1010`) and `model.rs:844–846` drops `finish`; `TurnStopReason` has no length variant (`turn.rs:91–101`); `reasoning_effort` is emitted only by the chat-completions dialect (`openai_compatible.rs:917–919`) and is inert on the messages dialect (`phase.rs:13–14`) |

Additional facts: the HTTP client is hand-rolled (`std::net::TcpStream` + rustls; `openai_compatible.rs:133–149, 477–700`) with no `HTTP_PROXY`/`HTTPS_PROXY` support; the provider transport dials directly and never touches `security::EgressProxy` (used only for MCP streamable HTTP, `exec_tools.rs:5985–6014`); `event_ledger::journal::EgressReceipt`/`record_egress` exist with **no callers** (`journal.rs:92–99, 640–646`); the ledger's `model.completed` payload carries a token total only (`interactive.rs:7871–7882`), no cost or input/output split; keyless configs send `Authorization: Bearer` with an empty token (`model.rs:112–119`, `openai_compatible.rs:2136`) although the reference page says no header is sent (`model-configuration.md:103–105`); the reference page names a peer product in its H1 and at lines 4, 22, 54, and `model-configuration.md:20,115` claim exit code 1, which is not in `JsonlExitCode::ALL` (an unconfigured model is `Runtime` = 5); an atomic temp+fsync+rename writer with a mode already exists (`exec_tools::atomic_write_with_mode`, `exec_tools.rs:4527–4578`) and is used for MCP settings at 0600 (`mcp_admin.rs:735–741`).

**Reachable:** TOML parsing, two adapters, config/env credential precedence, two retry layers, offline doctor, the atomic writer, `model.*` ledger events, goal budget accrual. **Unwired:** `FinishReason::Length`, `Responses` style, catalog prices, keychains, `EgressProxy` for providers, `EgressReceipt`. **Missing:** any config writer, presets, live probe, continuation, per-model retry keys, effort ids, reasoning summary, proxy support.

**Real remaining work:** `rapid setup` (wizard + flags) writing through `atomic_write_with_mode` at 0600 with a timestamped `.bak` and an `unchanged` outcome; a preset table (rule 2.2); a `keychain = "<alias>"` credential source alongside `env_key`, backed by `auth::production_backend`; a probe through the router that classifies auth/quota/network/server/invalid and records the first production `EgressReceipt`; `doctor --live` (doctor gains an argument parser); `[model.<id>]` keys `effort_ids`, `retry`, `reasoning_summary`, `continue_on_length` with the continuation loop in `SupervisedModel::step` reading the `finish` that `fold_stream` drops today and appending `model.continued` events; proxy variables in `Http1Transport`; doc corrections (key table, keyless header, exit codes, the H1). **Effort: L** (five serial slices; the probe's egress path is the only piece needing a decision — settled: `security::EgressProxy` + `journal::record_egress`, the two existing primitives).

### SEAM-03 — Background work as first-class graph nodes

| Claim in §4 | Verdict | Evidence |
|---|---|---|
| `shell_exec` supports background/detached execution | CONFIRMED | `ShellArgs { argv, timeout, background, sandbox }` (`exec_tools.rs:4628–4633`, parsed `:6396–6420`); `JobRegistry::start` (`:787–1023`) runs a supervisor thread, spools 64 KiB, polls at 100 ms; the model sees `job-N`, the ledger a `JobId`. The advertised schema (`:7328–7344`) lists only `argv` and `timeout_ms` as properties (`background`/`sandbox` are description text only); `DEFAULT_JOB_TIMEOUT` (`:94`) is unused — background jobs get the 60 s shell default unless `timeout_ms` is passed |
| the TUI has a `/jobs` panel and a job registry | CONFIRMED | `/jobs list|show|logs|cancel` (`crates/tui/src/commands.rs`); `AppState.jobs` (`state.rs:189`); `exec_tools::JobRegistry` |
| `process-supervisor` and `process-signal` exist | CONFIRMED; production use is narrow | `process_supervisor::{spawn, ExecSpec, await_exit_draining}` in `external_agents.rs:16–19`; `process_signal` in `eval_serve.rs:356–383`; its `JobRegistry`, `reconcile_orphans`, `OutputSpool`, `MonitorSpec`, `TriggerSpec` have **no** `apps/rapid` caller (`HostRuntime` is never constructed) |
| the scheduler has cron leases and wake-on-event (ADR 0015) | CONFIRMED for cron (`scheduler::PromptCron`, `cron.rs:117–260`; `rapid cron`, `p9_commands.rs:1607–1747`, a one-shot CLI tick with no background poller); **CORRECTED** for wake-on-event — ADR 0015 is a one-paragraph decision naming no types; the only wake code is `HostRuntime::drain_wake_signals` (`host_runtime.rs:79, 224, 242–248`), unwired | |
| no `/loop`, no demote keybinding, no `monitor` tool, no resume summary | CONFIRMED (all four) | TUI `CATALOG` (`commands.rs:445–614`) + host extras (`interactive.rs:5696–5724`); `map_crossterm` (`:10633–10653`); the fifteen `*_TOOL` constants (`exec_tools.rs:31–119`); `conversation_through` drops `job.*` (`:3872`) |
| whether `/jobs` rows are derived from ledger events — to verify | **VERIFIED: they are.** `AppState::apply_kernel` reduces `job.started/output/completed/orphan_reconciled` through `upsert_job` (`state.rs:691–763, 1097–1138`); `LedgerJobEvents` (`interactive.rs:7906–8035`) appends `job.started {job_id, handle, command}` and `job.completed {job_id, state, exit_status}`; `attach_ledger_sinks` (`:9792–9813`) is used by the TUI, ACP/daemon and `rapid exec` alike. `/jobs logs` and `/jobs cancel`, by contrast, go to the in-process registry (`:5910–5918`, `:6872–6883`) | |

Additional facts, each a real gap: (1) job lifetime is surface-dependent — the TUI's session-scoped `JobTable` outlives turns (`interactive.rs:4847, 8632`), but `rapid exec` and **every daemon/ACP turn** build a fresh `JobRegistry::default()` (`:8714, 8776`) whose `Drop` kills the children; (2) a detached `task_spawn` emits `job.started` but its worker never emits `finished` (`exec_tools.rs:668–670` vs `:3981–4108`), so its row is `started` forever; (3) `job.output`/`job.orphan_reconciled` have no producer, and no reconciliation runs at restart (`exec_tools.rs:586–589` says so); (4) ACP `session/update` drops `job.*` (`crates/acp/src/v1.rs:970–1016`) and the daemon's `SessionSnapshot` has no jobs field (`kernel/src/session/projection.rs:58–74`); (5) `rapid exec` JSONL emits `rapid.schema`, `router.decision`, `assistant.message`, `session.finished` only (`interactive.rs:4433–4590`); (6) notifications are an in-memory `SessionNotices` vector (`:4998–5046`), never recorded; (7) no `Process`/`Monitor`/`Trigger` graph node is ever created (`grep NodeKind::Monitor|Trigger|Process` → none); (8) completion is observed by the model through `drain_notifications` (`:1231–1270`) as a synthetic `background_jobs` exchange before each model step (`turn.rs:1290–1294`) — terminal jobs only.

**Reachable:** background `shell_exec`, detached `task_spawn`, `job_status`/`job_output`, `job.started/completed`, the ledger-derived `/jobs` reducer, `rapid cron`, graph `wait()` for approval steps. **Unwired:** `process_supervisor::{Monitor,Trigger,reconcile_orphans,OutputSpool}`, `HostRuntime`, `job.output`/`job.orphan_reconciled`, `NodeKind::{Process,Monitor,Trigger}`. **Missing:** job survival across daemon/ACP turns, `finished` for detached children, orphan reconciliation, job events in ACP and JSONL, `/loop`, `monitor`, demote, the resume block, recorded notifications, an interrupt marker.

**Real remaining work:** the item is reshaped by the audit. *Demote* is a keybinding and `/jobs bg <id>` over the existing background path (the process already runs on its own thread; "demote" is "stop waiting and let `drain_notifications` wake the turn"), plus the `job.*` transition it records. *Durable projection* is already true for `/jobs` rows; the work is to make it true everywhere: one session-scoped `JobRegistry` for daemon/ACP sessions, `finished` for detached children, `job.orphan_reconciled` at restart (its first producer, from `process_supervisor::reconcile_orphans`), `job.*` in ACP `session/update` and in `rapid exec --jsonl`. *`/loop`* wires `PromptCron` into the session with a lifetime (7 days) and a cap (50) stored on the row, a poller inside the session loop, results as recorded notifications (a new `notification.recorded` event — S6 makes the notice list a projection), and `/jobs` rows. *`monitor`* is a new tool over the existing job path with a per-line notification and flood control; `NodeKind::Monitor` is instantiated only when the session runs under the graph. *Resume summary* is a derived block over `job.*`/`agent.*`/cron rows injected through `host::build_packet_with` as a `jobs/running` system input. *Waits*: `subagent.wait_ceiling`/`job.wait_ceiling` default one hour, "still running". **Effort: L** (six slices; the daemon/ACP job lifetime is the only one that changes existing behaviour, disclosed).

### SEAM-04 — Subagent conversation control

| Claim in §4 | Verdict | Evidence |
|---|---|---|
| `crates/agent-pool`, `crates/handoff`, `crates/agent-runtime` (`role_profile.rs`/`specialist.rs`) exist | CONFIRMED for existence; **CORRECTED** for meaning — `agent-pool` is a warm *environment* pool (`ResourcePool`, `EnvironmentLease`; `crates/agent-pool/src/lib.rs:33–324`) and `handoff` is foreground→daemon ownership transfer (`crates/handoff/src/lib.rs:1–16`); neither has a production caller (`HostRuntime::new` is never constructed; no crate depends on `handoff`) | `grep` over `apps/rapid/src` and every `Cargo.toml` |
| a code-defined `RoleRegistry` | CONFIRMED | `role_profile.rs:124–274`: 13 roles, each with `tool_surface`, `model_policy`, `read_only`, `can_delegate`; no effort field |
| typed task/context envelopes | CONFIRMED in `agent-runtime` (`AgentSpec`, task/context types) — not in `agent-pool` | `LiveSubagentRunner::run` builds an `AgentSpec` (`interactive.rs:3027–3035`) |
| `/agents spawn|integrate|abandon` | **CORRECTED** — `integrate` and `abandon` exist (`interactive.rs:5711–5717` → `run_agent_view_command` `:5483–5594`); there is **no `/agents spawn`** (`crates/tui/src/commands.rs:1244–1267` parses `list|show|pause|resume|sleep|cancel|terminate`); spawning is the model-invoked `task_spawn` tool only | |
| managed `max_subagent_spawns_per_turn` | CONFIRMED | `managed_config.rs:184`; narrow-only via `.min()` (`exec_tools.rs:2043–2044`); default 32 (`:103`), enforced at `:3859` |
| no message delivery, no continuation, no file overlays, no admission queue | CONFIRMED for delivery (`SubagentRegistry` holds `(AgentId, CancellationToken)` only, `exec_tools.rs:419–482`) and continuation (documented unsupported, `:3976–3980`); **CORRECTED** for overlays — `.rapidlm/agents/*.toml` definitions exist (`agent_defs.rs`, closed schema `rapidlm.agent_defs.v1`, narrow-only `validate_grants` `:388`, builtins `explore`/`patch` `:429–453`) but the spawn path never reads them: the child is always `AgentRole::Coder` with the `subagent` permissions profile (`interactive.rs:3027–3035`) and `type ∈ AGENT_TYPES = ["general-purpose","explore","plan"]` (`exec_tools.rs:115`); CONFIRMED for admission — `MAX_DETACHED_SUBAGENTS = 4` is a rejecting counter (`:81`, `:485–493`), not a queue | |

Additional facts: `task_spawn` accepts a closed set `prompt, type, description, write_scope, background` (`parse_task_args`, `exec_tools.rs:6263–6306`) — nothing can widen the surface; inline children run on the parent's thread inside the tool call (`:3846–3961`), detached ones on a `std::thread` outliving the turn with the report in the job spool (`:3981–4085`); read-only children are an `ExecTools.read_only` flag (`:1856`), not a lattice mode; no per-subagent timeout exists (`--max-wall-time` reaches inline children through the bridged token, detached children deliberately ignore it, `:3972–3974`; `run_workflow_agent_step` uses a fresh token, `interactive.rs:10007`); `PersistentSpecialist` has a typed mailbox (`specialist.rs:172–188`) with no caller; `AgentMailSent`/`AgentMailDropped` event kinds have no emitter; the graph has `Supersedes`/`Invalidates` edges and `GraphProposal.supersede` (`proposal.rs:63–71`) but `/fork`/`/rewind` are ledger-session forks (`interactive.rs:7118–7204`), not graph revisions; `GraphService` has no `apps/rapid` caller outside `workflow_verified.rs`.

**Reachable:** `task_spawn` (inline + detached), spawn budget + managed ceiling, detached cap, `/agents cancel|integrate|abandon`, worktree isolation, `rapid agents list|validate|scaffold`. **Unwired:** `agent_defs` on the spawn path, `PersistentSpecialist` mailbox, `AgentMail*` events, `agent-pool`, `handoff`, `DelegationPolicy`. **Missing:** inbox delivery, continuation, admission queue, per-subagent wait ceiling, `/agents spawn`, `model`/`reasoning_effort`/`instructions`/`inputs`/`outputs` in definitions.

**Real remaining work** (ADR 0023): a durable inbox — `agent.mail.sent` gets its first emitter, a session-shared `Inbox` the child's turn loop drains at the three delivery points, and the transcript label; continuation as a new turn on the child's `AgentId` with its recorded history; admission through a bounded FIFO in `SubagentRegistry` instead of the rejecting counter, ceiling narrow-only from managed policy; `agent_type` resolved through `agent_defs::full_inventory` (definitions gain `base_role`, `instructions`, `model`, `reasoning_effort`, `inputs`, `outputs`; a user-level `~/.rapidlm/agents/` directory is added below the project one in precedence); declared outputs checked at completion; a one-hour default wait ceiling reported as "still running". **Effort: L.** Depends on SEAM-03's background-node work only for the "still running" wait semantics.

### SEAM-05 — Plan mode as a graph proposal

| Claim in §4 | Verdict | Evidence |
|---|---|---|
| No `/plan` | CONFIRMED for the slash command; **CORRECTED** for plan mode itself — two mechanisms exist | (1) tool-toggled: `plan_enter`/`plan_exit` (`exec_tools.rs:64,66`, `:3539–3598`) flip `ExecTools.plan_mode` and `permission_for` (`:2184–2190`) denies non-read-only calls except writes to `PLAN_PATH = ".rapidlm/plan.md"`; `plan_exit` reads the file and reports "Plan accepted" with no human decision; (2) lattice: `PermissionMode::Plan` denies every non-`ReadOnly` class before rules (`permissions.rs:590–592`) and again in the mode table (`:621`), reachable via `RAPIDLM_PERMISSION_MODE=plan`, settings `defaultMode`, ACP `session/set_mode` (`acp_serve.rs:85,126`) and the managed ceiling; it has no plan-file carve-out |
| the scheduler has a proposal type | CONFIRMED | `GraphProposal { base_revision, add_nodes, add_edges, supersede, invalidate }` (`proposal.rs:63–71`) |
| the playbook compiler turns steps into nodes | CONFIRMED | `playbook::compile` (`playbook.rs:205–208`); `workflow::PlaybookFile`/`Step` (`workflow.rs:58–81`) is the file shape `rapid run` loads |
| tools declare `read_only` in `crates/tool-gateway/src/schema.rs` | **CORRECTED** — no per-tool field; `"read_only"` is only a value of the `agent.spawn` tool's `access` enum (`schema.rs:154, 598–610`); `tool-gateway` has no dependent. The exec loop's classification is `tool_kind`/`tool_class` in `exec_tools.rs:4145–4162` | |
| human steps wait on the graph | CONFIRMED for `rapid run` (`workflow.rs:741–779` → `StepState::WaitingHuman { wait_token }`, `LedgerApprovalSink` `:1078`); `GraphService::wait/resume_wait` (`service.rs:326–357`) is reached only through `workflow_verified.rs` | |

Additional facts: `.rapidlm/plans/` does not exist; the exec loop never writes to `event_ledger::artifact_store` (only `rapid browser` does, `browser_cli.rs:249`); `plan_exit`'s "Plan accepted" is a model self-approval (S2 violation to correct).

**Real remaining work** (ADR 0024): make `/plan` and `rapid exec --plan` set the lattice's `Plan` mode; give the lattice the plan-file carve-out so the tool flag becomes a projection; `protocol::plan::PlanProposal` v1 + fixture; `plan.proposed/revised/approved/rejected` events; `plan_exit` submits through the approval wait; approval writes a `PlaybookFile` and starts a verified run (the ledger link plan → run → nodes); revisions under `.rapidlm/plans/<id>.md` with the artifact as the record; headless `--plan` exits `NeedsApproval`. **Effort: M** (the substrate is all present). Depends on SEAM-01's approval mapping.

### SEAM-08 — Worktree lifecycle

| Claim in §4 | Verdict | Evidence |
|---|---|---|
| `GitWorktreeStore`; `/fork`; `/agents spawn` creates child worktrees; `TransactionManager` publication | CONFIRMED for the store (`git_worktree.rs:225–417`: `open/create_view/remove_view/load_view/list_views`, never force-deletes, `CleanupFailed` state; worktrees under `<git-common-dir>/rapidlm/{views,worktrees}` `:45–48`); **CORRECTED** for `/fork` — it forks the ledger session (`interactive.rs:7118–7149`) and creates no worktree; **CORRECTED** for spawn — `task_spawn` write-capable children get a view through `AgentViewManager::create_for` (`agent_views.rs:133–163`), whose registry is an in-memory `HashMap` (`:124`) lost on restart; CONFIRMED for `TransactionManager` (`transaction.rs`: `Staged|Committed|RolledBack`, `begin/commit/materialize/rollback`; no `abandon` verb) | |
| no `--worktree` on `exec`/`goal`, no reclaim, no disk accounting | CONFIRMED | `parse_exec_args` (`interactive.rs:1973–2027`), `run_goal_command` flags (`:1182–1211`); no `reclaim|gc|prune` anywhere; `doctor.rs` has no size check |

Additional facts: `AgentViewManager::abandon` (`agent_views.rs:606–624`) removes the view without journaling; the publication journal records `workspace.publish` effects only (`publication.rs:52`); `PersistedWorktree` carries the view's `write_owner` but no session or lease id (`git_worktree.rs:128–136`); `RepoLease` is in-process only (`:713–731`); `RAPIDLM_HOME` (`user_home_from`, `interactive.rs:2056–2075`) holds `config.toml`, `project-trust.json`, `project-permissions.json` — there is no `worktrees/`, `sessions/`, `artifacts/` or `cache/` under it (sessions live in each project's `.rapidlm/sessions.sqlite`, `goal_host.rs:54`).

**Reachable:** the store, `AgentViewManager` create/integrate/abandon, journaled at-most-once publication. **Unwired:** `list_views()` metadata (no command shows it). **Missing:** `--worktree`, reclaim, disk accounting, journaled abandon, session/lease identity on the worktree, a `RAPIDLM_HOME/worktrees/` layout.

**Real remaining work:** the governing document places worktrees under `RAPIDLM_HOME/worktrees/<project-id>/<name>`; today they live inside the repository's `.git/rapidlm/worktrees`. **Decision needed** (recorded in the worklist): keep the in-repo location (no migration, `git worktree` semantics intact) and have `rapid worktree list|reclaim` and `du` read it, or move to the home layout. Recommendation: keep the in-repo location — it is what `git` expects and what publication already journals against — and read it from `du`. Then: `rapid exec --worktree [<name>]`/`rapid goal create --worktree` through `GitWorktreeStore::create_view`; journal abandon as a `workspace.abandon` effect; the reclaim rule over journal state + live registry + `git status --porcelain` in the worktree; `rapid worktree list|reclaim [--dry-run]`; base reuse (`git worktree add` from the local checkout is already network-free — the AC is a test, not a feature). **Effort: M.**

### SEAM-06 — Governance and distribution

| Claim in §4 | Verdict | Evidence |
|---|---|---|
| `RAPIDLM_MANAGED_CONFIG` points at an unsigned policy with the listed gates | CONFIRMED | `managed_config.rs:48,50`; closed `[policy]` set `:248–265` (`locked_default, allowed_providers, min_reasoning_effort, max_permission_mode, denied_tools, confine_writes_to, max_write_bytes_per_turn, max_fetch_bytes_per_turn, max_subagent_spawns_per_turn`); `load_policy` (`:643–673`) is a bounded read + TOML parse; `policy_version` is a non-cryptographic FNV-1a label (`:185–199`); gates report through `GateReportEntry { field_id, origin, detail, remediation }` (`:579–584`) |
| no MCP or plugin-source allow/deny list | CONFIRMED | zero hits for `allowed_servers|denied_servers|allowed_sources` |
| `rapid mcp list|get|add|remove|probe`, `mcp-tools`; `mcp_config.rs` reads one peer-host compatibility path | CONFIRMED | `mcp_admin.rs:179–205`; `PROJECT_SETTINGS_FILES` (`interactive.rs:2148–2149`) read at `mcp_config.rs:385–388`; `add` writes `.rapidlm/settings.json` at 0600 (`mcp_admin.rs:48, 60, 513, 561`) — **with no trust gate before the write** (trust is only an advisory note afterwards, `:592–597`; `probe` is gated, `:755–768`) |
| `crates/security` observes release signatures (`ReleaseSignatureObservation`) | CONFIRMED as an *observation enum*; **CORRECTED** — there is **no signature-verification primitive anywhere in the workspace** | `crates/security/src/doctor.rs:154–166`; no ed25519/minisign/asymmetric dependency in any `Cargo.toml` (only `sha2`); `rapid update` verifies a SHA-256 digest against the manifest (`update_serve.rs:167–171`) and the manifest's `signatures` field is documented (`:22–27`) but never parsed (`parse_manifest :57–80`); `doctor.rs:1245–1249` leaves signature material `NotProvided` |
| collision precedence and User-Agent presence to be verified | verified: tools are `mcp__<server>__<tool>` (`exec_tools.rs:1681`), dispatch is prefix-first (`:2417`) after exact built-in matches (`:2401–2416`), server names may not contain `__` (`mcp_config.rs:647–649`); the surface is appended after built-ins under a byte budget with **no name dedup** (`:7348–7377`); there are no plugin tools in the exec toolset at all. `User-Agent` is sent only by `http_get` (`rapidlm-web-fetch`, `openai_compatible.rs:2104`); the provider POST writer (`:2117–2141`) and the MCP HTTP transport (`mcp_http.rs:59–74`) send none | |
| no installer, no skill package | CONFIRMED | no `install` in `mcp_admin.rs`; `skills/` holds eight plain `.md` guides, no `SKILL.md`; `PluginInstaller::install` (`crates/plugin-host/src/install.rs:51–57`, staging/rollback/digest pin) has no caller — `rapid plugins register` only writes the trust store (`p9_commands.rs:2372–2382`) |

**Reachable:** managed gates and their report, `rapid mcp`, MCP naming, `rapid plugins validate|register|list|approve|reject|hook-test`. **Unwired:** `PluginInstaller`, `ReleaseSignatureObservation`. **Missing:** any asymmetric signature primitive, allow/deny lists, a trust gate on `mcp add`, dedup, User-Agent, installer, skill package.

**Real remaining work:** the signed-policy design assumes a primitive the repository does not have. **Decision needed** (worklist `SEAM-06-1`): add an asymmetric-signature dependency (recommendation: Ed25519 through a widely audited pure-Rust crate, detached signature file `<policy>.sig`, trusted public keys in `managed.trusted_keys`, and reuse the same verifier for the update manifest's documented-but-unparsed `signatures`) — a new dependency is a supply-chain decision for the user. Everything else proceeds: `managed.mcp.allowed_servers/denied_servers` and `managed.plugins.allowed_sources` enforced at `mcp add` (which also gains the missing trust gate — a disclosed behaviour change: an untrusted project's `mcp add` becomes a refusal that writes nothing), plugin install and session bind; deterministic collision reporting (built-in > plugin > MCP); `User-Agent: rapid/<version>` on the two writers; `rapid mcp install --into|--discover` by shape with format-preserving edits (JSONC/TOML/YAML — the repository has `serde_json` and `toml`; a comment-preserving JSONC/YAML editor is new code, so `--format jsonc|yaml` may need a tested minimal editor of its own — scoped in the task); `skill/SKILL.md` with a `--help`-drift test. **Effort: L** (signature slice blocked on the decision; the rest is M).

### SEAM-07 — Usage, disk and status-line observability

| Claim in §4 | Verdict | Evidence |
|---|---|---|
| `usage.rs` (`UsageAccumulator`, `AccountedCost`, `ModelUsage`) | CONFIRMED | `crates/llm-router/src/usage.rs:51–86` |
| goal budgets and usage in the goal snapshot | CONFIRMED | `goal_host.rs:699–755` (`accrue_turn_usage`) |
| `rapid insights`, `rapid inspect-export` | CONFIRMED — `insights` prints event-count heuristics only (`p9_commands.rs:3473–3475`); `inspect-export` prints `rapidlm.ledger_event` JSONL/md/html (`:3034–3041`) | |
| `crates/tui/src/status.rs` renders a fixed `StatusLine` (connectivity, context, policy, sandbox) | CONFIRMED, more items than listed | `StatusSnapshot { model, sandbox, policy, context, goal, agent_count, cost, connectivity }` (`:110–119`), items `:47–55` |
| `crates/telemetry` has OTLP-shaped records, `ExporterPolicy`, redaction, a local never-sent bundle | CONFIRMED; **no crate depends on `telemetry`** and the only `OtlpTransport` impls are test doubles (`lib.rs:908–954`) | |
| no `rapid usage`, `rapid du`, configurable status line, network exporter | CONFIRMED | `SUBCOMMANDS`; `user_config.rs:400` accepts only `models|model|phases`; `/context` exists (`commands.rs:494–499`) with a `used/cap` view model (`panels/context.rs:95–121`), `/usage` does not |

Additional facts: **the ledger carries no cost** — `model.completed` has `tokens: u64` only (`turn.rs:570–574`; `interactive.rs:7870–7882`); cost lives in-process (`CostAccumulator`, `host.rs:1019–1051`) and reaches `--usage-file`, the goal file and JSONL (`jsonl.rs:224–263`). So a ledger-derived `rapid usage` (S6) needs the `model.completed` payload to gain `input/output/cached` tokens and `AccountedCost` first. `run_sandboxed(root, argv, timeout, output_limit)` (`sandbox_exec.rs:314–321`) is the sandboxed command runner (host-restricted/seatbelt tier, network none); `RAPIDLM_HOME` holds `config.toml`, `project-trust.json`, `project-permissions.json` only — sessions, index, findings, plugins and goal files are per-project under `.rapidlm/`, and there is no caches directory.

**Real remaining work:** extend the `model.completed` payload additively (`input_tokens`, `output_tokens`, `cached_tokens`, `cost: AccountedCost`); `rapid usage` as a reducer over `model.*` events; `/usage` beside `/context`; `rapid du` over the per-project `.rapidlm/` tree plus `RAPIDLM_HOME` and the worktree store (the doc's `RAPIDLM_HOME`-centred layout is corrected to what exists); `[ui.status_line]` with a `protocol::status::StatusPayload` v1 fixture, run through `run_sandboxed`; `telemetry` gets its first dependent and a real `OtlpTransport` behind `[telemetry.otlp]`, egress through `EgressProxy` with `record_egress`. **Effort: M–L.**

### SEAM-10 — Agent-mode flags and error envelope

**CORRECTED throughout.** No global flag exists: `--output`, `--quiet`, `--non-interactive`, `--dry-run`, `--yes`, `--no-color`, `--timeout` have zero hits; each subcommand parses its own (`exec`: `--verbose --usage-file --resume --continue --max-wall-time --json-schema --jsonl`, `interactive.rs:1973–2011`; `browser --json`; `inspect-export --format`; `doctor` rejects `--json`, `p9_commands.rs:1273`). Errors are `eprintln!("{err}")` + `exit(err.exit_code())` (`main.rs:9–11`; `InteractiveError::exit_code` `interactive.rs:10970–10989`); P9 errors are printed and collapsed to `Usage` (`:525–533`); the only JSON error record is `JsonlRecord::error` in `--jsonl` mode (`jsonl.rs:269–283`). `session.created` carries `{ project_id }` only (`kernel/src/session/service.rs:52–54`) and both surfaces use `ActorKind::Human` — no origin tag. `rapid sessions list|search <text>` filters by id substring (`p9_commands.rs:1502–1574`).

**Real remaining work:** a shared `AgentModeFlags` parser applied in `run_subcommand` before dispatch, env mirrors, a `protocol::cli::CliError { code, message, hint }` envelope with fixture rendered on stderr in JSON mode, `hint` on every `InteractiveError` variant, `session.created.origin`, `sessions list --origin`. Exit codes unchanged. **Effort: M.**

### SEAM-11 — MCP elicitation and web search

CONFIRMED absent: the MCP client declares only `roots` and speaks `initialize`, `tools/list`, `tools/call` (`crates/mcp/src/transport.rs:63–64, 1099–1100, 1143–1144`); server-initiated frames are discarded (`:1066–1078`); no `web_search` tool; no `[toolset.*]` section. `web_fetch` has SSRF classification, an exact-host allowlist from settings and a 100 KB cap (`web_fetch.rs:14, 86–119`; `interactive.rs:2737–2746`) but **no ledger record or egress receipt and no lattice check** on its path (`exec_tools.rs:3812–3843`), and its success summary is cut to 256 bytes by `bounded_detail` (`:3832`). **Real remaining work:** elicitation as an input-required tool result mapped onto the approval wait (ADR 0022 §3); `web_search` behind a backend trait with typed unavailability when unconfigured; egress receipts on `web_fetch` while there. **Effort: M.** Depends on SEAM-01.

### SEAM-14 — Passive update notice

CONFIRMED: `rapid update [--url] [--force] [--check]` (`update_serve.rs:32–46`) requires `RAPIDLM_UPDATE_URL` (`:230, 254–257` — **there is no default manifest URL**, correcting "the existing manifest URL"), fetches through `curl -fsSL --proto =https` (`:98–111`), verifies SHA-256 (`:167–171`); no `[update] check`, no state file, no startup notice. **Real remaining work:** `[update] check`, a state file under `RAPIDLM_HOME`, the TTY/CI/headless gates, a 3 s budget, one line at exit, egress through the proxy with a receipt. **Decision needed:** there is no default manifest URL to check against; the notice is a no-op until `RAPIDLM_UPDATE_URL` or `[update] url` is configured (recommendation: `[update] url` in user config, no built-in default). **Effort: S.**

### SEAM-12 — Session ergonomics

CONFIRMED absent: `RAPIDLM_SESSION_ID`/`RAPIDLM_TURN_ID` appear nowhere in code — tool children, hooks and MCP servers receive exactly `PATH, HOME, LANG, TMPDIR` plus the platform base (`child_base_env`, `exec_tools.rs:5858–5869`; `hook_shell`, `hooks.rs:130–148`; `MCP_INHERITED_ENV`, `exec_tools.rs:5849`); no `/aside`, `/rename`, `/edit-prompt`, stash or recap. Existing: `/resume`, `/fork`, `/rewind`, `/compact`, `/queue`, `/model`, `/approvals`. **Real remaining work:** the two variables at the three spawn sites (a fourth once status commands exist); `/aside` as a read-only child (ADR 0023 §"Consequences") whose answer is shown and never appended to history — proven by a compiled-context test; the recap as a derived block beside SEAM-03's `jobs/running`; `/rename` (`session.renamed` event — the title is a projection); stash and `/edit-prompt` as TUI input-buffer operations. **Effort: M.** Depends on SEAM-03 (the derived-block injection point) and SEAM-01 (nothing hard; listed for the `user_prompt_submit` interaction with the stash).

### SEAM-13 — Memory consolidation

| Claim in §4 | Verdict | Evidence |
|---|---|---|
| the existing FTS + vector retrieval and file-watcher reindex are reused | **CORRECTED** — FTS exists (`context_fts` in the ledger, `migrations.rs:196`; `context-engine/src/index/fts.rs`), vector index types exist but have no `apps/rapid` caller (`EnabledVectorIndex`, `context-engine/src/lib.rs:68–72`), and there is **no file watcher** anywhere (no `notify`/inotify dependency; reindex is a per-turn walk in `context_retrieval::retrieve`, `context_retrieval.rs:71–115`; `/context reindex` answers "on-demand reindex is not wired", `interactive.rs:1779–1781`) | |
| the knowledge store | **CORRECTED** — `crates/knowledge` is a preference/feedback model (`FeedbackEvent`, `PreferenceCandidate`, `PreferenceStore`, `ExperimentRegistry`), declared in `apps/rapid/Cargo.toml:37` and never called; `/knowledge *` answers "no knowledge-candidate store exists yet" (`interactive.rs:1782–1785`) | |

Existing: `.rapidlm/MEMORY.md` bounded injection (`host.rs:1794–1806, 1939–1941`) and `/memory`; `/compact` → `context.compacted { summary, through_seq, turns }` (`interactive.rs:9507–9536`). **Real remaining work:** `/memory flush` writes typed records with evidence links into `context_engine::memory::MemoryStore` (its first production caller) and the `MEMORY.md` projection; `/memory consolidate` as a `PromptCron` job (the SEAM-03 loop path) that folds `context.compacted` summaries into topic notes as new revisions with provenance, never deleting sources; the "file-watcher reindex" is dropped from the item's text as non-existent — the per-turn walk stays. **Effort: M.** Depends on SEAM-03.

### SEAM-15 — Agent dashboard

Nothing exists; the daemon exposes `events.subscribe` (raw events from a seq) and `sessions.get` (`daemon_serve.rs:306–308, 426–461`). The dashboard is a reducer over the same events the TUI folds, hosted by the daemon. Deferred behind SEAM-03 and SEAM-04 as the governing document says. **Effort: M.**

## 4. What this changes in the plan

1. **The catalog's "today" paragraphs were wrong in eleven places** (recorded above); none of them widens the program, several shrink it: plan mode's read-only floor exists; `/jobs` rows already derive from the ledger; `.rapidlm/agents/*.toml` overlays exist; a cron facade exists; the approval wait is durable and shared. The remaining work in SEAM-03/04/05 is wiring and reaching, exactly as `newtask.md` §0a predicts.
2. **Two items are larger than written.** SEAM-06's signed policy has no primitive to reuse (a dependency decision); SEAM-07's `rapid usage` cannot be ledger-derived until the ledger records cost (an additive payload change comes first).
3. **Three decisions are needed and recorded** (§5); each item proceeds on every slice that does not depend on its decision.
4. **Order within Tier 1 stays catalog order.** SEAM-01 first (SEAM-05, SEAM-09, SEAM-11 depend on its approval mapping), SEAM-02 second (independent, and it changes the getting-started path), SEAM-03 third, SEAM-04 fourth (depends on SEAM-03's wait semantics), SEAM-05 fifth.
5. **Every "today" correction migrates into the governing document** through the commit that carries this audit (§0.3), as strike-and-replace of the affected sentences with a pointer to this file; the acceptance criteria are untouched.

## 5. Decisions needed (recorded, not blocking)

| Id | Item | Question | Options | Recommendation |
|---|---|---|---|---|
| D-1 | SEAM-06 | Signed managed policy needs an asymmetric-signature primitive the workspace lacks | (a) add an Ed25519 dependency (pure Rust, widely audited) and a `managed.trusted_keys` set, reusing the verifier for the update manifest's documented `signatures`; (b) HMAC with a shared secret from the keychain (symmetric — anyone who can verify can sign); (c) defer signing by ADR | (a); it is the only option that gives fail-closed verification without a shared secret. Until decided, `require_signature` is not implemented and every other SEAM-06 slice proceeds |
| D-2 | SEAM-08 | Worktree location: the document says `RAPIDLM_HOME/worktrees/<project-id>/<name>`; the store puts them under the repository's `.git/rapidlm/worktrees` | (a) keep in-repo, make `du`/`worktree list` read it; (b) move to the home layout | (a); it is what `git worktree` expects and what publication already journals against |
| D-3 | SEAM-14 | There is no default update-manifest URL to check | (a) `[update] url` in user config, no built-in default; the notice is a no-op until set; (b) bake a URL into the binary | (a); a built-in URL is an egress destination the user did not choose |

## 6. Where the other Phase 0 artifacts are

- **ADR 0022** — [Hook result v2](../adrs/0022-hook-result-v2-decisions-rewrites-and-context.md): the versioned result, `ask` on the existing approval wait, rewrites with provenance, fenced context, the new stages, managed gates.
- **ADR 0023** — [Inbox edges and delivery modes](../adrs/0023-inbox-edges-and-subagent-delivery-modes.md): `agent.mail.*` as the record, three delivery points in the child's turn loop, continuation as a superseding turn, FIFO admission, definitions on the spawn path.
- **ADR 0024** — [Plan proposals](../adrs/0024-plan-proposals-approved-into-the-graph.md): the lattice mode, the proposal artifact, approval through the wait, compilation through the playbook path.
- **Worklist** — [`seams-implementation-tasks.json`](seams-implementation-tasks.json): SEAM-00-1/2/3 recorded `complete` with this document and the ADRs as evidence; every other task `pending` with explicit dependencies.
- **Development ledger** — `docs/development-ledger.md` gains the `SEAMS` section.
