# Observability and SLOs

Track kernel health, graph queue/latency, context compile/retrieval, tool validation/repair, process/resource/sandbox, model/provider, Computer Use, policy/approvals, persistence, artifacts and client stream. SLO candidates are established from Phase 0 baselines; alert on correctness signals (event append failures, journal uncertain backlog, stale evidence escapes, duplicate jobs) before optimizing cosmetic latency.
