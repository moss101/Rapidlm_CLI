# Release and Supply Chain

Release requires clean source revision, locked dependencies, security/license scans, generated SBOM, cross-platform tests, signed binaries/manifests, provenance attestation, updater verification and rollback test. CI credentials are least-privilege/short-lived. Third-party code copying requires license/notice review; preferred competitive adoption is architecture/pattern reimplementation.
