# Definition of Done

A task is VERIFIED only when implementation matches current repository contract; acceptance criteria and relevant error/boundary/recovery paths are covered; configured gates were observed passing; security/persistence/cancellation implications are handled; current evidence is recorded; diff is scoped; no required evidence is stale; no blocking contradiction remains; and the development ledger names the evidence. “Code written,” “command invoked,” or an agent self-assessment is insufficient.
