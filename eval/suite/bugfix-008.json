{
  "id": "bugfix-008",
  "category": "bugfix",
  "prompt": "test_lists.py currently fails. Read lists.py, find and fix the bug (known symptom: chunk([1, 2, 3], 2) drops the remainder [3]; it must keep it), and make test_lists.py pass. Do not change the test.",
  "setup": {
    "lists.py": "def chunk(xs, n):\n    return [xs[i:i+n] for i in range(0, len(xs), n) if len(xs[i:i+n]) == n]\n",
    "test_lists.py": "from lists import chunk\nassert chunk([1, 2, 3], 2) == [[1, 2], [3]]\nprint('OK')\n"
  },
  "gold": {
    "lists.py": "def chunk(xs, n):\n    return [xs[i:i+n] for i in range(0, len(xs), n)]\n"
  },
  "verify": "python3 -B test_lists.py",
  "verify_fails_before": true
}
