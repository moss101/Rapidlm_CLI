{
  "id": "multifile-011",
  "category": "multifile",
  "prompt": "The requirement changed: config.TIMEOUT must be 60 (it is 30). Update config.py, keep client.py and worker.py working, and make the check pass: python3 -B -c 'import config, sys; sys.exit(0 if config.TIMEOUT == 60 else 1)' && python3 -B test_config.py. Do not modify test_config.py.",
  "setup": {
    "config.py": "TIMEOUT = 30\n",
    "client.py": "import config\n\ndef value():\n    return config.TIMEOUT\n",
    "worker.py": "import config\n\ndef current():\n    return config.TIMEOUT\n",
    "test_config.py": "from client import value\nfrom worker import current\nassert value() == current(), 'modules disagree'\nprint('OK')\n"
  },
  "gold": {
    "config.py": "TIMEOUT = 60\n"
  },
  "verify": "python3 -B -c 'import config, sys; sys.exit(0 if config.TIMEOUT == 60 else 1)' && python3 -B test_config.py",
  "verify_fails_before": true,
  "protected": [
    "test_config.py"
  ]
}
