{
  "id": "multifile-014",
  "category": "multifile",
  "prompt": "The requirement changed: config.FEATURE_ON must be True (it is False). Update config.py, keep client.py and worker.py working, and make the check pass: python3 -B -c 'import config, sys; sys.exit(0 if config.FEATURE_ON == True else 1)' && python3 -B test_config.py",
  "setup": {
    "config.py": "FEATURE_ON = False\n",
    "client.py": "import config\n\ndef value():\n    return config.FEATURE_ON\n",
    "worker.py": "import config\n\ndef current():\n    return config.FEATURE_ON\n",
    "test_config.py": "from client import value\nfrom worker import current\nassert value() == current(), 'modules disagree'\nprint('OK')\n"
  },
  "gold": {
    "config.py": "FEATURE_ON = True\n"
  },
  "verify": "python3 -B -c 'import config, sys; sys.exit(0 if config.FEATURE_ON == True else 1)' && python3 -B test_config.py",
  "verify_fails_before": true
}
