{
  "id": "multifile-015",
  "category": "multifile",
  "prompt": "The requirement changed: config.GREETING must be \"hello\" (it is \"hi\"). Update config.py, keep client.py and worker.py working, and make the check pass: python3 -B -c 'import config, sys; sys.exit(0 if config.GREETING == \"hello\" else 1)' && python3 -B test_config.py",
  "setup": {
    "config.py": "GREETING = \"hi\"\n",
    "client.py": "import config\n\ndef value():\n    return config.GREETING\n",
    "worker.py": "import config\n\ndef current():\n    return config.GREETING\n",
    "test_config.py": "from client import value\nfrom worker import current\nassert value() == current(), 'modules disagree'\nprint('OK')\n"
  },
  "gold": {
    "config.py": "GREETING = \"hello\"\n"
  },
  "verify": "python3 -B -c 'import config, sys; sys.exit(0 if config.GREETING == \"hello\" else 1)' && python3 -B test_config.py",
  "verify_fails_before": true
}
