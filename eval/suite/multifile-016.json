{
  "id": "multifile-016",
  "category": "multifile",
  "prompt": "The requirement changed: config.LIMIT must be 50 (it is 5). Update config.py, keep client.py and worker.py working, and make the check pass: python3 -B -c 'import config, sys; sys.exit(0 if config.LIMIT == 50 else 1)' && python3 -B test_config.py",
  "setup": {
    "config.py": "LIMIT = 5\n",
    "client.py": "import config\n\ndef value():\n    return config.LIMIT\n",
    "worker.py": "import config\n\ndef current():\n    return config.LIMIT\n",
    "test_config.py": "from client import value\nfrom worker import current\nassert value() == current(), 'modules disagree'\nprint('OK')\n"
  },
  "gold": {
    "config.py": "LIMIT = 50\n"
  },
  "verify": "python3 -B -c 'import config, sys; sys.exit(0 if config.LIMIT == 50 else 1)' && python3 -B test_config.py",
  "verify_fails_before": true
}
