{
  "id": "multifile-018",
  "category": "multifile",
  "prompt": "The requirement changed: config.VERSION must be 2 (it is 1). Update config.py, keep client.py and worker.py working, and make the check pass: python3 -B -c 'import config, sys; sys.exit(0 if config.VERSION == 2 else 1)' && python3 -B test_config.py. Do not modify test_config.py.",
  "setup": {
    "config.py": "VERSION = 1\n",
    "client.py": "import config\n\ndef value():\n    return config.VERSION\n",
    "worker.py": "import config\n\ndef current():\n    return config.VERSION\n",
    "test_config.py": "from client import value\nfrom worker import current\nassert value() == current(), 'modules disagree'\nprint('OK')\n"
  },
  "gold": {
    "config.py": "VERSION = 2\n"
  },
  "verify": "python3 -B -c 'import config, sys; sys.exit(0 if config.VERSION == 2 else 1)' && python3 -B test_config.py",
  "verify_fails_before": true,
  "protected": [
    "test_config.py"
  ]
}
