{
  "id": "tests-026",
  "category": "tests",
  "prompt": "textutil.py has no tests. Write test_textutil.py that imports every public function (wrap, indent) and asserts their documented behavior, then make it pass.",
  "setup": {
    "textutil.py": "def wrap(s, n):\n    return s[:n]\n\n\ndef indent(s):\n    return '  ' + s\n\n"
  },
  "gold": {
    "test_textutil.py": "from textutil import wrap, indent\nassert wrap(\"abcdef\", 3) == \"abc\"\nassert indent(\"x\") == \"  x\"\nprint('OK')\n"
  },
  "verify": "python3 -B test_textutil.py",
  "verify_fails_before": true
}
